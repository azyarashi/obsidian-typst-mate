import { setIcon } from 'obsidian';

export const markdownIconEl = createDiv();
setIcon(markdownIconEl, 'notebook-text');

export const typstIconEl = createDiv();
setIcon(typstIconEl, 'typst-stroke');

export const settingsIconEl = createDiv();
setIcon(settingsIconEl, 'settings');
